# Sanguo Panel 测试用例

## 1. 基础架构与认证 (P0)

### TC-101: 后端基础 API 连通性
- **描述**: 验证后端服务是否正常启动，基础状态接口是否可用。
- **步骤**:
  1. 访问 `http://localhost:8000/api/v1/system/status`
- **预期结果**: 返回 HTTP 200，内容包含 `"status": "online"`。
- **实际结果**: 通过 (2026-02-12)

### TC-102: 管理员登录
- **描述**: 验证管理员账号是否可以正常登录并获取 JWT Token。
- **步骤**:
  1. 使用账号 `admin` 密码 `admin123` 调用 `/api/v1/login/access-token`
- **预期结果**: 返回 HTTP 200，包含 `access_token`。
- **实际结果**: 通过 (2026-02-12)

### TC-103: 系统监控接口
- **描述**: 验证系统监控数据接口是否正常返回硬件状态。
- **步骤**:
  1. 携带有效的 Token 调用 `/api/v1/system/metrics`
- **预期结果**: 返回包含 CPU、内存、磁盘使用率的 JSON 数据。
- **实际结果**: 通过 (2026-02-12)

## 2. 前端界面与布局 (P0)

### TC-201: 登录页面展示
- **描述**: 验证前端登录页面是否正常渲染。
- **步骤**:
  1. 访问 `http://localhost:5173/login`
- **预期结果**: 显示 Arco Design 风格的登录卡片。
- **实际结果**: 通过 (2026-02-12)

### TC-202: 全局布局渲染
- **描述**: 验证登录后的主布局（左侧导航、顶部状态栏）是否正常显示。
- **步骤**:
  1. 登录成功后跳转至 `/dashboard`
- **预期结果**: 看到左侧菜单（Dashboard, Websites, System）和顶部状态栏。
- **实际结果**: 通过 (2026-02-12)

## 3. 站点管理 (P0)

### TC-301: 获取站点列表
- **描述**: 验证能否从后端获取站点列表。
- **步骤**:
  1. 访问 `Websites` 页面。
- **预期结果**: 表格显示站点数据（初始为空或包含默认测试数据）。
- **实际结果**: 通过 (2026-02-12)

### TC-302: 创建新站点
- **描述**: 验证创建站点功能。
- **步骤**:
  1. 点击 `Create Site` 按钮。
  2. 输入域名 `test.com`，根路径 `/www/wwwroot/test.com`，选择默认共享数据库。
  3. 点击确定。
- **预期结果**: 提示创建成功，列表刷新并显示 `test.com`。
- **实际结果**: 通过 (2026-02-12)

### TC-303: 删除站点
- **描述**: 验证删除站点功能。
- **步骤**:
  1. 在站点列表中点击 `Delete` 按钮。
  2. 在气泡确认框中点击确定。
- **预期结果**: 提示删除成功，列表刷新且该站点消失。
- **实际结果**: 通过 (2026-02-12)

## 4. LiteSpeed 管理 (P1)

### TC-401: 获取服务状态
- **描述**: 验证能否获取 LiteSpeed 服务状态。
- **步骤**:
  1. 访问 `LiteSpeed` 页面。
- **预期结果**: 显示当前服务状态（Running/Stopped）。
- **实际结果**: 通过 (2026-02-12, Mocked on Windows)

### TC-402: 重启服务
- **描述**: 验证重启 LiteSpeed 功能。
- **步骤**:
  1. 点击 `Restart` 按钮。
- **预期结果**: 提示重启成功。
- **实际结果**: 通过 (2026-02-12, Mocked on Windows)

## 5. PHP 管理 (P1)

### TC-501: 多版本 PHP 状态展示
- **描述**: 验证能否显示多个版本的 PHP 状态。
- **步骤**:
  1. 访问 `PHP` 页面。
- **预期结果**: 显示 8.2, 8.1, 7.4 三个版本的卡片。
- **实际结果**: 通过 (2026-02-12)

### TC-502: 重启指定版本 PHP
- **描述**: 验证重启特定版本 PHP 的功能。
- **步骤**:
  1. 在 PHP 8.2 卡片上点击 `Restart`。
- **预期结果**: 提示重启成功，调用 `/api/v1/services/lsphp82/restart`。
- **实际结果**: 通过 (2026-02-12, Mocked on Windows)

## 6. MariaDB 管理 (P1)

### TC-601: 管理共享数据库实例
- **描述**: 验证管理员能否管理共享数据库实例。
- **步骤**:
  1. 访问 `MariaDB` 页面。
  2. 点击 `Add Instance`。
  3. 输入测试数据库信息并保存。
- **预期结果**: 列表显示新添加的实例。
- **实际结果**: 通过 (2026-02-12)

### TC-602: MariaDB 服务状态与操作
- **描述**: 验证能否查看 MariaDB 服务状态并执行重启操作。
- **步骤**:
  1. 在 MariaDB 页面查看服务状态。
  2. 点击 `Restart Service`。
- **预期结果**: 状态显示 Running，重启提示成功。
- **实际结果**: 通过 (2026-02-12, Mocked on Windows)

## 7. Redis & 缓存管理 (P1)

### TC-701: Redis 服务操作
- **描述**: 验证 Redis 重启与清空功能。
- **步骤**:
  1. 访问 `Redis` 页面。
  2. 点击 `Restart`。
  3. 点击 `Flush All Keys`。
- **预期结果**: 重启与清空均提示成功。
- **实际结果**: 通过 (2026-02-12)

### TC-702: LSCache 清理
- **描述**: 验证 LSCache 全局清理功能。
- **步骤**:
  1. 点击 `Purge All LSCache`。
- **预期结果**: 提示清理成功。
- **实际结果**: 通过 (2026-02-12)

## 8. 安全管理 (P1)

### TC-801: 修改管理员密码
- **描述**: 验证能否修改面板管理员密码。
- **步骤**:
  1. 访问 `Security` 页面。
  2. 输入旧密码和新密码。
  3. 点击 `Update Password`。
- **预期结果**: 提示更新成功。
- **实际结果**: 通过 (2026-02-12, Mocked)

### TC-802: 面板安全设置
- **描述**: 验证能否保存面板安全配置（如 Token 有效期、IP 白名单）。
- **步骤**:
  1. 修改 Token 有效期。
  2. 点击 `Save Security Settings`。
- **预期结果**: 提示保存成功。
- **实际结果**: 通过 (2026-02-12, Mocked)

## 9. 系统管理 (P1)

### TC-901: 查看系统日志
- **描述**: 验证能否正常查看面板系统日志。
- **步骤**:
  1. 访问 `System` 页面，切换到 `System Logs` 标签。
- **预期结果**: 显示日志内容。
- **实际结果**: 通过 (2026-02-12)

### TC-902: 检查更新
- **描述**: 验证能否检查面板版本更新。
- **步骤**:
  1. 切换到 `Update` 标签。
  2. 点击 `Check for Updates`。
- **预期结果**: 提示当前已是最新版本。
- **实际结果**: 通过 (2026-02-12)

## 10. 监控统计 (P2)

### TC-1001: 资源趋势图表
- **描述**: 验证能否正常显示 CPU、内存及网络流量趋势图。
- **步骤**:
  1. 访问 `Monitor` 页面。
- **预期结果**: ECharts 图表正常渲染，且 CPU/内存数据每 2 秒更新一次格式。
- **实际结果**: 通过 (2026-02-12)

## 11. 一键部署 (P2)

### TC-1101: WordPress 一键安装
- **描述**: 验证能否在指定站点一键安装 WordPress。
- **步骤**:
  1. 访问 `Apps` 页面。
  2. 点击 WordPress 卡片的 `Install Now`。
  3. 选择一个站点并填写管理员信息。
  4. 点击确认。
- **预期结果**: 提示安装成功。
- **实际结果**: 通过 (2026-02-12, Mocked)

## 12. 备份管理 (P2)

### TC-1201: 手动备份
- **描述**: 验证能否手动创建站点备份。
- **步骤**:
  1. 访问 `Backup` 页面。
  2. 点击 `Create Manual Backup`。
  3. 选择备份目标并确认。
- **预期结果**: 提示备份任务已在后台启动。
- **实际结果**: 通过 (2026-02-12, Mocked)

### TC-1202: 定时备份配置
- **描述**: 验证能否配置自动备份计划。
- **步骤**:
  1. 切换到 `Scheduled Backup` 标签。
  2. 修改频率和存储位置。
  3. 点击 `Save Schedule`。
- **预期结果**: 提示保存成功。
- **实际结果**: 通过 (2026-02-12)

## 13. 缓存控制中心 (P0)

### TC-1301: 性能预设切换
- **描述**: 验证能否通过三级滑块切换性能预设（Basic/Balanced/Ultimate）。
- **步骤**:
  1. 访问 `Cache` 页面。
  2. 拖动滑块至 `Ultimate`。
- **预期结果**: 提示预设已更新，且下方详细开关（LSCache, Redis 等）自动同步开启。
- **实际结果**: 通过 (2026-02-12)

### TC-1302: 缓存清理
- **描述**: 验证针对特定站点的缓存清理功能。
- **步骤**:
  1. 选择一个站点。
  2. 点击 `Purge All Cache`。
- **预期结果**: 提示清理成功，调用 `/api/v1/cache/sites/{id}/purge`。
- **实际结果**: 通过 (2026-02-12)

## 14. SSL 自动管理 (P1)

### TC-1401: 申请 Let's Encrypt 证书
- **描述**: 验证能否为站点申请 SSL 证书并开启强制 HTTPS。
- **步骤**:
  1. 访问 `SSL` 页面。
  2. 点击某个站点的 `Apply SSL`。
  3. 勾选 `Force HTTPS` 并确认。
- **预期结果**: 提示申请成功，状态变为 `Force HTTPS`。
- **实际结果**: 通过 (2026-02-12, Mocked)

### TC-1402: 证书状态检查
- **描述**: 验证能否获取证书的详细信息（颁发者、到期时间）。
- **步骤**:
  1. 点击 `Check Status`。
- **预期结果**: 弹窗显示颁发者（Let's Encrypt）和到期时间。
- **实际结果**: 通过 (2026-02-12)

## 15. 图片优化模块 (P1)

### TC-1501: 图片优化统计查看
- **描述**: 验证能否查看站点的图片优化统计信息。
- **步骤**:
  1. 访问 `Image` 页面。
  2. 选择一个站点。
- **预期结果**: 显示总图片数、已优化数、节省空间等信息。
- **实际结果**: 通过 (2026-02-12)

### TC-1502: 启动批量优化
- **描述**: 验证能否配置并启动图片批量优化任务。
- **步骤**:
  1. 调整质量滑块至 85%。
  2. 勾选 `Convert to WebP`。
  3. 点击 `Start Optimization`。
- **预期结果**: 提示优化已开始，并在下方日志框中显示模拟任务步骤。
- **实际结果**: 通过 (2026-02-12)

## 16. 数据库高级管理 (P1)

### TC-1601: 慢查询日志查看
- **描述**: 验证能否在面板中查看数据库慢查询记录。
- **步骤**:
  1. 访问 `DatabaseAdv` 页面。
  2. 切换到 `Slow Queries` 标签。
- **预期结果**: 显示慢查询 SQL 语句、执行时间和时间戳。
- **实际结果**: 通过 (2026-02-12)

### TC-1602: 一键优化数据库
- **描述**: 验证能否对特定数据库进行一键优化。
- **步骤**:
  1. 切换到 `Optimization & Tables` 标签。
  2. 选择一个数据库。
  3. 点击 `One-Click Optimize`。
- **预期结果**: 弹出优化结果，显示清理的操作和回收的空间。
- **实际结果**: 通过 (2026-02-12)

## 17. 前端资源优化 (P1)

### TC-1701: 资源优化状态检查
- **描述**: 验证能否获取站点的 CSS/JS/字体等优化状态。
- **步骤**:
  1. 访问 `Assets` 页面。
  2. 选择一个站点。
- **预期结果**: 显示 CSS 是否压缩、JS 是否压缩、字体是否本地化等。
- **实际结果**: 通过 (2026-02-12)

### TC-1702: 执行资源优化
- **描述**: 验证能否执行 CSS/JS 合并压缩和 Critical CSS 生成。
- **步骤**:
  1. 勾选 `Minify CSS` 和 `Minify JS`。
  2. 点击 `Run Asset Optimization`。
- **预期结果**: 提示优化完成，并显示节省的文件大小。
- **实际结果**: 通过 (2026-02-12)

## 18. CDN 一键接入 (P2)

### TC-1801: CDN 服务商连接
- **描述**: 验证能否通过 API Key 连接 Cloudflare 等 CDN 服务商。
- **步骤**:
  1. 访问 `CDN` 页面。
  2. 输入 API Key 并点击 `Verify & Connect`。
- **预期结果**: 提示连接成功，显示分配的 CNAME 地址。
- **实际结果**: 通过 (2026-02-12, Mocked)

### TC-1802: CDN 缓存全局刷新
- **描述**: 验证能否触发 CDN 全局缓存刷新。
- **步骤**:
  1. 点击 `Purge Global Cache`。
- **预期结果**: 提示刷新请求已发送。
- **实际结果**: 通过 (2026-02-12)

## 19. 一键全站极速优化 (P2)

### TC-1901: 启动全自动化优化流程
- **描述**: 验证能否通过“一键优化”按钮自动执行所有预设的优化步骤。
- **步骤**:
  1. 访问 `Ultimate` 页面。
  2. 选择一个站点并点击 `RUN ULTIMATE OPTIMIZATION`。
- **预期结果**: 步骤条依次点亮，显示正在进行的任务（图片、缓存、数据库等），最后提示完成。
- **实际结果**: 通过 (2026-02-12)
